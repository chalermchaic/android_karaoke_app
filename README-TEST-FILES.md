# 🎬 Test Video Files for Android Karaoke App

## วิธีดาวน์โหลดไฟล์ทดสอบ

### Windows:
```bash
download-test-files.bat
```

### Linux/Mac:
```bash
chmod +x download-test-files.sh
./download-test-files.sh
```

## 📹 ไฟล์ที่จะดาวน์โหลด

ไฟล์วิดีโอ MP4 มาตรฐานที่รับรองว่าเล่นได้บน ExoPlayer:

### ไฟล์ขนาดเล็ก (สำหรับทดสอบเบื้องต้น)
1. **sample-small.mp4** (531 KB) - ไฟล์ทดสอบขนาดเล็ก
2. **sample-1mb.mp4** (1 MB) - วิดีโอสั้น 5 วินาที
3. **sample-10mb.mp4** (10 MB) - วิดีโอสั้น 10 วินาที

### ไฟล์คุณภาพสูง (Google Sample Videos)
4. **big-buck-bunny-720p.mp4** - วิดีโอการ์ตูน Big Buck Bunny (720p)
5. **elephants-dream-720p.mp4** - วิดีโอการ์ตูน Elephants Dream (720p)
6. **for-bigger-blazes-720p.mp4** - วิดีโอตัวอย่าง (720p)

## 🚀 ขั้นตอนการทดสอบ

### 1. ดาวน์โหลดไฟล์
```bash
# Windows
download-test-files.bat

# Linux/Mac
./download-test-files.sh
```

### 2. Copy ไฟล์ไปยัง Android Device

#### วิธีที่ 1: ใช้ ADB (แนะนำ)
```bash
# ตรวจสอบว่าเชื่อมต่ออุปกรณ์แล้ว
adb devices

# Copy ไฟล์ทั้งหมด
adb push test-files/*.mp4 /sdcard/Movies/

# หรือ copy ทีละไฟล์
adb push test-files/sample-small.mp4 /sdcard/Movies/
adb push test-files/big-buck-bunny-720p.mp4 /sdcard/Movies/
```

#### วิธีที่ 2: Copy ผ่าน USB Cable
1. เชื่อมต่อ Android กับคอมพิวเตอร์ผ่าน USB
2. เลือก "File Transfer" (MTP mode)
3. เปิด Internal Storage → Movies
4. Copy ไฟล์ .mp4 จากโฟลเดอร์ test-files

### 3. ทดสอบในแอพ

1. **เปิดแอพ Karaoke**
2. **กดปุ่ม "Rescan Files"** เพื่อสแกนไฟล์ใหม่
3. **กดปุ่ม "Test Files"** (สีส้ม) เพื่อทดสอบทุกไฟล์
4. **ตรวจสอบผลลัพธ์:**
   - ✅ = เล่นได้
   - ❌ = เล่นไม่ได้ (พร้อมข้อความ error)

### 4. ทดสอบการเล่น
1. กลับไปหน้าหลัก
2. เล่นวิดีโอ
3. กดปุ่ม "Next Song" เพื่อเปลี่ยนวิดีโอ
4. ทดสอบปุ่ม "Vocal: ON/OFF" (อาจไม่เห็นผลเพราะไม่ใช่ไฟล์คาราโอเกะ)

## 📝 หมายเหตุสำคัญ

### ⚠️ ไฟล์เหล่านี้ไม่ใช่ไฟล์คาราโอเกะ
- ✅ ใช้ทดสอบ**การเล่นวิดีโอ**ได้
- ✅ ใช้ทดสอบ**การสลับเพลง**ได้
- ❌ **ไม่มีเนื้อเพลง**แสดงบนหน้าจอ
- ❌ ปุ่ม **Vocal ON/OFF** อาจไม่เห็นผล

### ✨ สำหรับไฟล์คาราโอเกะจริง
ต้องหาไฟล์คาราโอเกะที่มี:
- เนื้อเพลงฝังอยู่ในวิดีโอ (CDG, VCD format)
- หรือมี 2 audio tracks (track 1 = ดนตรี, track 2 = ดนตรี+เสียงร้อง)

## 🌐 แหล่งไฟล์

**ไฟล์ทดสอบจาก:**
- [Learning Container](https://www.learningcontainer.com/mp4-sample-video-files-download/)
- [Google Sample Videos](https://goo.gle/cmg-test-videos)
- [Sample Lib](https://download.samplelib.com/)

**แหล่งอื่นๆ สำหรับไฟล์ทดสอบ:**
- [File Examples](https://file-examples.com/index.php/sample-video-files/sample-mp4-files/)
- [The Test Data](https://thetestdata.com/sample-mp4-file-download.php)
- [Free Test Data](https://freetestdata.com/video-files/mp4/)
- [Test File](https://testfile.org/category/video-test-file/video-mp4-format/)

## 🔧 Troubleshooting

### ถ้าไฟล์เล่นไม่ได้:
1. ตรวจสอบว่าไฟล์อยู่ใน `/sdcard/Movies/`
2. ตรวจสอบ permissions (READ_MEDIA_VIDEO)
3. ดู error message ในหน้า "Test Files"
4. ลองใช้ไฟล์ขนาดเล็กก่อน (sample-small.mp4)

### ถ้า ADB ใช้ไม่ได้:
```bash
# ตรวจสอบว่าติดตั้ง ADB แล้ว
adb version

# เปิด USB debugging บน Android:
# Settings → About Phone → กด "Build Number" 7 ครั้ง
# Settings → Developer Options → เปิด "USB Debugging"
```

### ถ้าแอพไม่เห็นไฟล์:
1. ตรวจสอบ permissions ของแอพ
2. กด "Rescan Files" อีกครั้ง
3. Restart แอพ
4. ตรวจสอบว่าไฟล์อยู่ที่ถูกต้อง: `/sdcard/Movies/`
